<h1>Destinations#show</h1>
<p> <%= @destination.name %><br>
  <%= @destination.country %><br>
  <label>Featured Post:</label>
  <%= link_to @destination.featured_post.title, post_path(@destination.featured_post) %><br>
  <label>Average Blogger Age (for some reason): </label>
  <%= @destination.ave_age %><br>
  <label>Recent Posts:</label>
  <ul>
  <% @destination.most_recent.each do |post| %>
  <li>
    <%= post.title %>
  </li>
  <% end %>
</ul>

</p>
